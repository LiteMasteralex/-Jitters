Programa myProgram;
var int : i, j, k;
  float : pi[5][5];
  char : stop[10];

funcion void voidFunction();
  var int : subI;
  {
    si(x < 50)
    entonces {
      x = y;
    }
    sino {
      function();
    }

# TODO(LARA) : también causa error, sospecho que es por el " | "
#    mientras((X < 10) | (Y > z)) haz {
#      y = 10;
#    }

    desde x=0; hasta Y | Z hacer {
      j = 8;
    }
  }

  funcion float floatFunction();
  {
    id();
    regresa(5); # TODO(LARA): verificar si queremos que 
                #             termine con ";" esta funcion
  }



principal() {
  j = 10;
  x = 5.2;
  # msg = 'c'; <--- TODO(LARA) : CAUSA PROBLEMA, revisar lexer.py sobre char  
  myId = Funcion() + FuncionOtra(x, y, z) + 24;
  last = (10 * 5) / 8;

  lee(x, y, z);
  escribe("Result: ", id);
}
