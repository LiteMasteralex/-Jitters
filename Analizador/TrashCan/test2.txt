Programa myProgram;
var int : i, j, k;
  
# Compentario de la voidFunction()
funcion void voidFunction();
  var int : subI, x, y, z;
      float : X, Y, Z;
  {
    si(x < 50)
    entonces {
      x = y;
    }
    sino {
      voidFunction();
    }

    mientras((X < 10) | (Y > z)) haz {
      y = 10;
    }

    desde x=0; hasta Y hacer {
      j = 8;
    }
  }

funcion float floatFunction();
  {
    voidFunction();
    regresa(5.0);
  }



principal() {
  j = 10;
  voidFunction();
  k = 24;
  lee(i, j, k);
  escribe("Result: ", j);
}
