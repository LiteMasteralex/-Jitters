Programa myProgram;
var int : i, j, k;
  
funcion int factorial(int:x);
  {
      si(x ~= 0) entonces{
          regresa(1);
      }
      sino{
          regresa(x * factorial(x - 1));
      }
  }

principal() {
  escribe(factorial(6));
}